# evento 
-onClik-> clic del usuario 
-onChange -> cuando escribe en un input 
-onSubmit -> cuando envia un formulario 
-onMouseEnter -> pasa el mause 

<button></onclick="HacerAlgo()"></button> SINTASIS HTML 
<button></onclick="{HacerAlgo}"></button> SINTASIS REACT 


-useState -> hook 
    -crear una variable de estado 
    -crea una funcion para actualizar algo
    -le dice a react: "cuando cambiar"

const [valor, setValor]= useState(valorInicial);
conts [contador, setContador]= useState(0);

-contador -> valor actual
-setContador -> funcion que actualiza
-0 -> valor inicial 